/**
 * @file
 *
 * User-facing notice texts shared by every entry point, so the same failure reads the same way whether it
 * was reached from a click, a context menu, or an editor command.
 */

/**
 * Shown when the gesture named a link the plugin could not find in the source note.
 */
export const COULD_NOT_LOCATE_LINK_NOTICE = 'Could not locate the link in the source note.';
