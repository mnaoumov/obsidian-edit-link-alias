# Project Rules

## Known Issues

- Build fails with `TS2403` in `@types/css-font-loading-module` — type conflict between `obsidian-typings` → `pixi.js` → `@types/css-font-loading-module` and `better-typescript-lib`. Upstream fix needed in `obsidian-typings`.

## Current Task

None.
